package asmlib.lex.symbols;

/**
 * A {@link Symbol} which contains a special character
 * 
 * @param character
 * @author Mechafinch
 */
public record SpecialCharacterSymbol(char character) implements Symbol {
    
}
