package asmlib.token.tokens;

/**
 * A {@link Token} which contains a special character
 * 
 * @param character
 * @author Mechafinch
 */
public record SpecialToken(char character) implements Token {
    
}
